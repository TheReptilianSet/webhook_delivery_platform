# Fix Phase Verification — 2026-08-27

## Scope

This pass addressed the gaps found during the specification review: production configuration,
tenant-safe constraints, static migrations, timeout and retry behavior, bounded retention, API
contracts, dependency-aware readiness, telemetry, and CI execution of integration/e2e tests.

## Passed

| Area | Command or check | Result |
| --- | --- | --- |
| Frozen dependencies | `uv sync --all-groups --frozen`; `uv lock --check` | Passed |
| Formatting and lint | `uv run ruff format --check .`; `uv run ruff check .` | 124 files, passed |
| Types | `uv run mypy src tests` | 105 source files, no issues |
| Unit/API/security | `uv run pytest -m "not integration and not e2e" --strict-markers -q` | 71 passed |
| Full non-e2e | Test image with real PostgreSQL and RabbitMQ | 76 passed, 2 deselected |
| Compose e2e | `pytest -m e2e --strict-markers -q` in the test service | 2 passed, 76 deselected |
| Migration round trip | Alembic `upgrade head`, `downgrade base`, `upgrade head` | Passed on clean `webhook_migration_fix` |
| Tenant constraint | Cross-organization endpoint-secret reference | Rejected by PostgreSQL |
| Local acceptance flow | `uv run python scripts/local_acceptance_flow.py` | Success, retry/DLQ, and replay passed |
| Poison command | Invalid versioned delivery command | Rejected and observed in RabbitMQ dead queue |
| Compose configuration | Default and production-like overrides, `config --quiet` | Passed |
| Runtime image | `docker compose build api` | Passed; final non-root runtime image built |
| Dependency audit | `uv run pip-audit` | No known third-party dependency vulnerabilities |

The test suite emits an upstream FastAPI/Starlette `TestClient` deprecation warning. The poison
message test also emits an AMQP library warning about a deprecated datetime helper. Neither warning
changes test behavior.

## Failed, Resolved, or Not Run

- The first acceptance-flow run after adding event metrics returned `500` because the counter was
  called with the wrong label name. The metric call was corrected and the complete flow passed on
  rerun.
- The first host-side Alembic attempt was interrupted by Docker Desktop port forwarding. The same
  migration was then exercised inside the isolated Compose network and passed the full round trip.
- `pip-audit` was blocked by sandbox network policy on its first attempt; the approved networked run
  completed successfully.
- Trivy was not run locally. The pinned filesystem/image scan remains a CI gate.
- CI was not executed remotely. Load, prolonged fault injection, backup/restore, and SLO measurements
  were not run, so no production-readiness or performance claim is made.

## Remaining Operational Limits

- Rate limiting remains process-local and is suitable only for a single API replica.
- DNS is checked at registration and before each call, but production still needs an egress policy.
- Delivery is at least once; the documented uncertainty window can produce duplicates.
- The production-like Compose override verifies port exposure only. It is not a deployment profile.
